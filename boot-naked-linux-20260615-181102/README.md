# Boot Naked Linux — generate a minimal initramfs for bare-metal Linux boot.

*Auto-generated by APSA-Net Teacher Agent — 2026-06-15 14:14*

## Overview

Boot Naked Linux — generate a minimal initramfs for bare-metal Linux boot.

"Naked Linux" means booting the kernel directly into a shell with zero
userspace bloat: no systemd, no udev, no dbus. Just /bin/sh and you.

This tool scaffolds a minimal root filesystem, writes a bare init script,
and packs

| Metric | Value |
|--------|-------|
| Files | 1 |
| Lines of code | 385 |
| Functions | 12 |
| Classes | 0 |
| Dependencies | 0 packages |

## Quick Start

```bash
python main.py --help
```

## Functions

| Signature | Description |
|-----------|-------------|
| `_ensure_dir(path: Path)` | — |
| `_write_text(path: Path, content: str, executable: bool)` | — |
| `cmd_scaffold(rootfs_dir: Path)` | Create a minimal root filesystem skeleton. |
| `_cpio_pad(size: int)` | CPIO newc format: align to 4-byte boundaries. |
| `_cpio_newc_header(inode: int, mode: int, uid: int)` | Build a CPIO newc (SVR4 without CRC) header: 110 bytes + nam |
| `_pack_cpio_archive(rootfs_dir: Path, archive_fh)` | Write a CPIO newc archive of rootfs_dir to archive_fh.  Retu |
| `cmd_pack(rootfs_dir: Path, output: Path)` | Pack a rootfs directory into a gzipped cpio initramfs. |
| `cmd_inspect(archive: Path)` | Peek inside an initramfs archive (pure Python). |
| `cmd_info()` | Print bootloader / kernel config reference. |
| `_human_size(n: int)` | — |
| `build_parser()` | — |
| `main()` | — |


## Project Structure

```
boot-naked-linux-20260615-181102/

---

## Support

Part of [APSA-Net](https://github.com/flipperspectives-crypto/apsa-builds) — autonomous pipeline building open-source tools from real-world problems.

```
SOL: BmGW3LNPG9RbL6SCoPMCHQckyy4u3ovg6gLFDCzgEwnZ
```
