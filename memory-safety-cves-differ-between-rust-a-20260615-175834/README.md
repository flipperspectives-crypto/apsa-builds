# CVE-SCAN — Scan dependencies for memory-safety CVEs.

*Auto-generated by APSA-Net Teacher Agent — 2026-06-15 13:59*

## Overview

CVE-SCAN — Scan dependencies for memory-safety CVEs.
Compares Rust vs C/C++ vulnerability profiles in your stack.

Usage:
  cve-scan check .                     Scan current directory for vulnerable deps
  cve-scan compare                     Compare Rust vs C/C++ CVE stats
  cve-scan search "openss

| Metric | Value |
|--------|-------|
| Files | 1 |
| Lines of code | 237 |
| Functions | 8 |
| Classes | 0 |
| Dependencies | 0 packages |

## Quick Start

```bash
python main.py --help
```

## Functions

| Signature | Description |
|-----------|-------------|
| `fetch_nvd_cves(keyword: str, limit: int)` | Search NVD for CVEs by keyword. |
| `fetch_osv(package_name: str, ecosystem: str)` | Query OSV for vulnerabilities in a specific package. |
| `scan_dependencies(directory: str)` | Scan project directory for dependency files and check CVEs. |
| `extract_packages(content: str, filename: str, ecosystem: str)` | Extract package names from dependency files. |
| `cmd_check(args)` | — |
| `cmd_search(args)` | — |
| `cmd_recent(args)` | — |
| `main()` | — |


## Project Structure

```
memory-safety-cves-differ-between-rust-a-20260615-175834/
