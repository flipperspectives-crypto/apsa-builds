# emufix — Pattern-based binary patcher.

*Auto-generated by APSA-Net Teacher Agent — 2026-06-16 07:39*

## Overview

emufix — Pattern-based binary patcher.

Inspired by the x86 emulator team that detected known-broken instruction
sequences at runtime and fixed them on the fly.  This CLI tool does the
same thing offline: scan a binary file for byte patterns (bad code) and
replace them with corrected sequences (the 

| Metric | Value |
|--------|-------|
| Files | 1 |
| Lines of code | 258 |
| Functions | 13 |
| Classes | 0 |
| Dependencies | 1 packages |

## Quick Start

```bash
pip install -r requirements.txt
python main.py --help
```

## Functions

| Signature | Description |
|-----------|-------------|
| `_hex_to_bytes(hex_str: str)` | Parse space-separated hex like '90 90 EB FE' into bytes. |
| `_bytes_to_hex(data: bytes)` | — |
| `_find_all(data: bytes, pattern: bytes)` | Return list of byte offsets where *pattern* appears in *data |
| `_apply_patches(data: bytearray, offsets: List[int], old_len: int)` | Replace *old_len* bytes at each offset with *new* (pads/trun |
| `cmd_scan(path: Path, hex_pattern: str)` | — |
| `cmd_patch(path: Path, hex_find: str, hex_fix: str)` | — |
| `cmd_diff(path: Path, hex_find: str, hex_fix: str)` | — |
| `cmd_list()` | — |
| `cmd_export(out_path: Optional[Path])` | Export built-in patterns as JSON so users can extend them. |
| `_resolve_pattern(candidate: str)` | If candidate names a built-in pattern, return its 'find' str |
| `_resolve_replacement(candidate: str)` | — |
| `build_parser()` | — |


## Project Structure

```
the-time-the-x86-emulator-team-found-cod-20260616-113815/
